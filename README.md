# Конвертер физических величин С3

Простой и удобный конвертер физических величин на JavaScript, выполненный в виде одного HTML-файла. Позволяет конвертировать массы в силу и энергию, а также рассчитывать объём и плотность.

## ✨ Возможности

- **Конвертация массы в силу**: расчёт силы по формуле F = m × g (g = 9.81 м/с²)
- **Конвертация массы в энергию**: расчёт энергии по формуле Эйнштейна E = m × c²
- **Расчёт объёма**: вычисление объёма по массе и плотности (V = m / ρ)
- **Расчёт плотности**: вычисление плотности по массе и объёму (ρ = m / V)
- **Переключатель темы**: светлая и тёмная тема с сохранением выбора
- **Журнал действий**: автоматическое сохранение всех расчётов с временными метками
- **Современный интерфейс**: красивый дизайн с плавными анимациями
- **Адаптивный дизайн**: корректно работает на разных размерах экрана
- **Локальное хранилище**: настройки темы и журнал сохраняются между сеансами

## 🚀 Быстрый старт

1. Откройте файл `index.html` в любом современном браузере (Chrome, Firefox, Edge, Safari)
2. Введите необходимые значения в поля ввода
3. Нажмите соответствующую кнопку для выполнения расчёта
4. Результаты отобразятся ниже, а действие запишется в журнал справа

## 📊 Физические формулы

### Конвертация массы в силу
```
F = m × g
```
где:
- **F** — сила (Н, ньютоны)
- **m** — масса (кг)
- **g** — ускорение свободного падения на Земле (9.81 м/с²)

### Конвертация массы в энергию
```
E = m × c²
```
где:
- **E** — энергия (Дж, джоули)
- **m** — масса (кг)
- **c** — скорость света (299,792,458 м/с)

Результат также отображается в килоджоулях (кДж) и мегаджоулях (МДж).

### Расчёт объёма
```
V = m / ρ
```
где:
- **V** — объём (м³)
- **m** — масса (кг)
- **ρ** — плотность (кг/м³)

Результат также отображается в литрах (л).

### Расчёт плотности
```
ρ = m / V
```
где:
- **ρ** — плотность (кг/м³)
- **m** — масса (кг)
- **V** — объём (м³)

Результат также отображается в граммах на кубический сантиметр (г/см³).

## ⌨️ Управление

### Мышь
- Введите значения в соответствующие поля
- Нажмите кнопку для выполнения расчёта:
  - **Конвертировать в Силу** — вычисляет силу по массе
  - **Конвертировать в Энергию** — вычисляет энергию по массе
  - **Рассчитать Объём** — вычисляет объём по массе и плотности
  - **Рассчитать Плотность** — вычисляет плотность по массе и объёму
- Используйте переключатель темы в правом верхнем углу для изменения оформления
- Просматривайте историю расчётов в журнале действий справа
- Очистите журнал кнопкой **Очистить** при необходимости

### Клавиатура
- **Enter** в поле массы — конвертация в силу
- **Enter** в поле объёма — расчёт плотности
- **Enter** в поле плотности — расчёт объёма

## 🎯 Примеры использования

1. **Конвертация массы в силу:**
   - Введите массу: `10` кг
   - Нажмите "Конвертировать в Силу"
   - Результат: `98.1` Н

2. **Конвертация массы в энергию:**
   - Введите массу: `0.001` кг (1 грамм)
   - Нажмите "Конвертировать в Энергию"
   - Результат: `8.9876e+13` Дж

3. **Расчёт объёма:**
   - Введите массу: `1000` кг
   - Введите плотность: `1000` кг/м³ (плотность воды)
   - Нажмите "Рассчитать Объём"
   - Результат: `1` м³ или `1000` л

4. **Расчёт плотности:**
   - Введите массу: `7850` кг
   - Введите объём: `1` м³
   - Нажмите "Рассчитать Плотность"
   - Результат: `7850` кг/м³ или `7.85` г/см³

## 💻 Технологии

- **HTML5** — структура страницы
- **CSS3** — стилизация, анимации и адаптивный дизайн
- **JavaScript (Vanilla)** — логика конвертера и работа с localStorage
- **LocalStorage API** — сохранение темы и журнала действий

## 📋 Структура проекта

```
c3_L/
├── index.html  # Основной файл конвертера
├── README.md   # Документация проекта
└── LICENSE     # Лицензия
```

## 🔧 Особенности реализации

- Весь код содержится в одном файле — легко использовать и делиться
- Нет внешних зависимостей — работает без интернета
- Чистый JavaScript — без фреймворков и библиотек
- Сохранение состояния — тема и журнал действий сохраняются в браузере
- Умное форматирование чисел — автоматическое использование научной нотации для больших значений
- Ограничение журнала — сохраняются последние 50 записей
- Валидация ввода — проверка корректности введённых значений

## 🌓 Переключение темы

Конвертер поддерживает две темы:
- **Светлая тема** — по умолчанию, комфортная для работы днём
- **Тёмная тема** — удобна для работы в тёмное время суток

Выбранная тема автоматически сохраняется и будет использоваться при следующем открытии страницы.

## 📝 Журнал действий

Все выполненные расчёты автоматически сохраняются в журнале действий, который расположен справа от основного интерфейса. Для каждой записи сохраняется:
- Временная метка
- Тип операции
- Входные данные
- Результат расчёта
- Использованная формула

Журнал сохраняется в localStorage браузера и доступен после перезагрузки страницы. Максимальное количество записей — 50.

## 📱 Совместимость

Конвертер работает во всех современных браузерах:
- Google Chrome (рекомендуется)
- Mozilla Firefox
- Microsoft Edge
- Safari
- Opera

## 📄 Лицензия

См. файл [LICENSE](LICENSE) для подробной информации о лицензии.

---

**Приятных расчётов! 🚀**
